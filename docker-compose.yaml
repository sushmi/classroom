version: '3.9'

services:
  dash:
    command: python3 main.py
    image: sushmi/ait-ml-dash:20251116
    platform: linux/x86_64
    build: 
      context: .
      dockerfile: .Dockerfile
    ports:
      - 9001:80
    volumes:
      - ./src/app:/app
      - ./.vscode-server:/root/.vscode-server
    environment:
      - HOST=0.0.0.0
      - PORT=80